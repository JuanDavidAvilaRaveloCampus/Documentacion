/**
 * *MÉTODOS DE CLASE
 *  Las funciones se pueden definir como métodos de clase en una clase 
 *  utilizando la sintaxis clase.
*/

class calculator {
    sum (a , b) {
        return a + b;
    }
};