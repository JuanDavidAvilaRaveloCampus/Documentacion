/**
 * *METODOS DE OBJETO
 *  Una función se define como un método de un objeto utilizando la sintaxis de 
 *  notación de punto.
 */ 

const calculador = {
    sum: function (a, b) {
        return a + b ;
    }
};